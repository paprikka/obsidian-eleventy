<!doctype html>
<html lang="en">
  <head>
    {% include 'meta.njk' %}
    <link rel="stylesheet" href="/assets/main.css" />
    {% include 'umami.njk' %}
  </head>
  <body {% if pageClass %}class="page-{{ pageClass }}" {% endif %}>
    {{ content | safe }}
  </body>
</html>
